import { View, Text } from "react-native";
import React from "react";

export default function Paginator({ data }) {
  return (
    <View className="absolute flex-row bottom-60">
      <Text>Paginator</Text>
    </View>
  );
}
